{
  "version": "0.0.1",
  "name": "nectar",
  "main": "src/index.ts",
  "scripts": {
    "start": "npm run copy-mocks && tsc",
    "watch": "npm run copy-mocks && tsc --watch",
    "test": "jest",
    "test-watch": "jest --watch",
    "schemas-to-types": "ts-node ./schemasToTypes.ts",
    "copy-mocks": "mkdir -p ./build/src/testMocks && cp -R ./src/testMocks/ ./build/src/testMocks/"
  },
  "dependencies": {
    "joi": "^17.6",
    "joi-to-typescript": "^4.0.5",
    "mailchimp-api-v3": "^1.15.0"
  },
  "devDependencies": {
    "@types/jest": "^28.1.6",
    "@types/node": "^18.6.2",
    "jest": "^28.1.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  }
}
